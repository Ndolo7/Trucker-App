from .base import *

# Override base settings with development-specific settings here
DEBUG = True

ALLOWED_HOSTS = ['*']

# Optional: Add development apps like debug-toolbar
# INSTALLED_APPS += ['debug_toolbar']
